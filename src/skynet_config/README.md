# Skynet_config

This package is a central repository for all the parameter files used by the robot in operation.
Parametes files such as nav2 related configs, custom files declaring the variables.

This will allow the user to modify the parameters without compiling the source code.

## List of all parameters and brief explanation


- paramters/xyz.yaml
